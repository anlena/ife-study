// 将设置放入此文件中以覆盖默认值和用户设置。
{
    "autoprefixer.browsers": [
        "last 10 versions",
        "ie >= 8",
        "ie_mob >= 10",
        "ff >= 30",
        "chrome >= 34",
        "safari >= 6",
        "ios >= 6",
        "android >= 4.4"
    ]
}